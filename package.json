{
  "name": "train-pain",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "update": "git pull && npm install && pm2 restart TRAIN-PAIN && pm2 logs TRAIN-PAIN --lines 50"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "socket.io": "^3.1.1"
  }
}
